spring:
  jpa:
    database-platform: org.hibernate.dialect.MySQLMyISAMDialect
    open-in-view: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
logging:
  level:
    web: debug
    org:
      hibernate:
        SQL: DEBUG
